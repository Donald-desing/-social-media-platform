{
  "name": "social-media-platform",
  "version": "0.3.0", // Updated version
  "private": true,
  "homepage": ".",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1", // Or latest if you are comfortable with it
    "react-router-dom": "^6.21.1", // Or latest if you are comfortable with it
    "firebase": "^10.7.1", // Or latest if you are comfortable with it
    "@jridgewell/sourcemap-codec": "^1.5.0", // Or latest
    "@rollup/plugin-terser": "^7.0.0", // Or latest
    "glob": "^9.0.0", // Or latest
    "rimraf": "^5.0.0", // Or latest
    "lru-cache": "^7.0.0"  // Or latest if you actually need it
  },
  "devDependencies": {
    "eslint": "^8.0.0", // Or latest
    "@eslint/object-schema": "^2.0.0", // Or latest
    "@eslint/config-array": "^1.0.0", // Or latest
    "@babel/core": "^7.0.0", // Or latest
    "@babel/preset-env": "^7.0.0", // Or latest
    "@babel/preset-react": "^7.0.0", // Or latest
    "svgo": "^3.0.0" // Or latest
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
